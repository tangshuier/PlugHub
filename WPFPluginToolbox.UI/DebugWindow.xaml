<Window x:Class="WPFPluginToolbox.UI.DebugWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFPluginToolbox.UI"
        mc:Ignorable="d"
        Title="调试窗口" Height="600" Width="800"
        WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- 工具栏 -->
        <ToolBar Grid.Row="0">
            <Button Content="清空日志" Click="ClearLogs_Click"/>
            <Separator/>
            <Label Content="日志级别:"/>
            <ComboBox x:Name="LogLevelComboBox" Width="120">
                <ComboBoxItem Content="全部" IsSelected="True" Tag="All"/>
                <ComboBoxItem Content="信息" Tag="Info"/>
                <ComboBoxItem Content="警告" Tag="Warning"/>
                <ComboBoxItem Content="错误" Tag="Error"/>
                <ComboBoxItem Content="调试" Tag="Debug"/>
            </ComboBox>
            <Button Content="筛选" Click="FilterLogs_Click"/>
        </ToolBar>
        
        <!-- 日志列表 -->
        <DockPanel Grid.Row="1" Margin="2">
            <DataGrid x:Name="LogsDataGrid" AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="时间" Binding="{Binding Timestamp}" Width="180"/>
                    <DataGridTextColumn Header="级别" Binding="{Binding Level}" Width="80"/>
                    <DataGridTextColumn Header="消息" Binding="{Binding Message}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>
    </Grid>
</Window>