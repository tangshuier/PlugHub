<UserControl x:Class="PerformanceTestPlugin.PerformanceTestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Background="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- 标题 -->
        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" 
                   Text="性能监控测试" FontSize="24" FontWeight="Bold" 
                   Margin="0 0 0 20" />

        <!-- 操作名称输入 -->
        <Label Grid.Row="1" Grid.Column="0" Content="操作名称:" VerticalAlignment="Center" />
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="OperationNameTextBox" Margin="10 0" Height="30" 
                 Text="TestOperation" />

        <!-- 测试次数输入 -->
        <Label Grid.Row="2" Grid.Column="0" Content="测试次数:" VerticalAlignment="Center" />
        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" Margin="10 10 0 10" Height="30">
            <TextBox x:Name="TestCountTextBox" Width="100" Text="1000" />
            <TextBlock Text="次" Margin="10 0" VerticalAlignment="Center" />
        </StackPanel>

        <!-- 按钮区域 -->
        <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" 
                    Orientation="Horizontal" Margin="0 10 0 20">
            <Button Content="执行耗时操作" x:Name="ExecuteButton" Click="ExecuteButton_Click" Width="120" Height="30" Margin="0 0 10 0" />
            <Button Content="开始计时" x:Name="StartTimerButton" Click="StartTimerButton_Click" Width="100" Height="30" Margin="0 0 10 0" />
            <Button Content="停止计时" x:Name="StopTimerButton" Click="StopTimerButton_Click" Width="100" Height="30" />
        </StackPanel>

        <!-- 结果显示 -->
        <GroupBox Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" Header="操作日志" Margin="0 20 0 0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="ResultTextBlock" TextWrapping="Wrap" />
            </ScrollViewer>
        </GroupBox>
    </Grid>
</UserControl>