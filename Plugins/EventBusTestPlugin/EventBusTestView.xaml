<UserControl x:Class="EventBusTestPlugin.EventBusTestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Background="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- 标题 -->
        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" 
                   Text="事件总线测试" FontSize="24" FontWeight="Bold" 
                   Margin="0 0 0 20" />

        <!-- 事件类型选择 -->
        <Label Grid.Row="1" Grid.Column="0" Content="事件类型:" VerticalAlignment="Center" />
        <ComboBox Grid.Row="1" Grid.Column="1" x:Name="EventTypeComboBox" Margin="10 0" Height="30">
            <ComboBoxItem Content="自定义事件" Tag="CustomEvent" />
            <ComboBoxItem Content="错误事件" Tag="ErrorEvent" />
            <ComboBoxItem Content="配置变更事件" Tag="ConfigChangedEvent" />
        </ComboBox>

        <!-- 事件内容输入 -->
        <Label Grid.Row="2" Grid.Column="0" Content="事件内容:" VerticalAlignment="Top" />
        <TextBox Grid.Row="2" Grid.Column="1" x:Name="EventContentTextBox" Margin="10 10 0 10" 
                 Height="60" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" />

        <!-- 按钮区域 -->
        <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" 
                    Orientation="Horizontal" Margin="0 10 0 20">
            <Button Content="发布事件" x:Name="PublishButton" Click="PublishButton_Click" Width="100" Height="30" Margin="0 0 10 0" />
            <Button Content="订阅事件" x:Name="SubscribeButton" Click="SubscribeButton_Click" Width="100" Height="30" Margin="0 0 10 0" />
            <Button Content="取消订阅" x:Name="UnsubscribeButton" Click="UnsubscribeButton_Click" Width="100" Height="30" />
        </StackPanel>

        <!-- 事件日志显示 -->
        <GroupBox Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" Header="事件日志" Margin="0 20 0 0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="EventLogTextBlock" TextWrapping="Wrap" />
            </ScrollViewer>
        </GroupBox>
    </Grid>
</UserControl>