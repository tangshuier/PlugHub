<UserControl x:Class="DataShareTestPlugin.DataShareTestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Background="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- 标题 -->
        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" 
                   Text="数据共享测试" FontSize="24" FontWeight="Bold" 
                   Margin="0 0 0 20" />

        <!-- 数据键输入 -->
        <Label Grid.Row="1" Grid.Column="0" Content="数据键名:" VerticalAlignment="Center" />
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="KeyTextBox" Margin="10 0" Height="30" />

        <!-- 数据值输入 -->
        <Label Grid.Row="2" Grid.Column="0" Content="数据值:" VerticalAlignment="Center" />
        <TextBox Grid.Row="2" Grid.Column="1" x:Name="ValueTextBox" Margin="10 10 0 10" Height="30" />

        <!-- 按钮区域 -->
        <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" 
                    Orientation="Horizontal" Margin="0 10 0 20">
            <Button Content="存储数据" x:Name="StoreButton" Click="StoreButton_Click" Width="100" Height="30" Margin="0 0 10 0" />
            <Button Content="获取数据" x:Name="RetrieveButton" Click="RetrieveButton_Click" Width="100" Height="30" Margin="0 0 10 0" />
            <Button Content="检查数据" x:Name="CheckButton" Click="CheckButton_Click" Width="100" Height="30" Margin="0 0 10 0" />
            <Button Content="删除数据" x:Name="DeleteButton" Click="DeleteButton_Click" Width="100" Height="30" />
        </StackPanel>

        <!-- 结果显示 -->
        <Label Grid.Row="4" Grid.Column="0" Content="结果:" VerticalAlignment="Center" />
        <TextBlock Grid.Row="4" Grid.Column="1" x:Name="ResultTextBlock" 
                   Margin="10 0" Height="30" VerticalAlignment="Center" 
                   Foreground="Green" FontWeight="Bold" />

        <!-- 日志显示 -->
        <GroupBox Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2" Header="操作日志" Margin="0 20 0 0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="LogTextBlock" TextWrapping="Wrap" />
            </ScrollViewer>
        </GroupBox>
    </Grid>
</UserControl>