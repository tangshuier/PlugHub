<UserControl x:Class="ConfigTestPlugin.ConfigTestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400"
             d:DesignWidth="600"
             Background="White">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock FontSize="24" FontWeight="Bold" Margin="0,0,0,20">配置测试插件</TextBlock>
            
            <StackPanel Margin="0,0,0,20">
                <TextBlock FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10">选项配置</TextBlock>
                <TextBlock Margin="0,0,0,5">当前选中的选项:</TextBlock>
                <ComboBox x:Name="OptionComboBox" Width="200" Margin="0,0,0,10" SelectionChanged="OptionComboBox_SelectionChanged">
                    <ComboBoxItem Content="选项一" Tag="0"/>
                    <ComboBoxItem Content="选项二" Tag="1"/>
                    <ComboBoxItem Content="选项三" Tag="2"/>
                    <ComboBoxItem Content="选项四" Tag="3"/>
                    <ComboBoxItem Content="选项五" Tag="4"/>
                </ComboBox>
                <TextBlock Foreground="Gray" FontSize="12" Margin="0,5,0,0">
                    选择一个选项，点击保存按钮保存配置。下次打开插件时会记住当前选择。
                </TextBlock>
            </StackPanel>
            
            <StackPanel Margin="0,20,0,0">
                <Button x:Name="SaveButton" Content="保存配置" Width="120" Height="35" Click="SaveButton_Click"/>
            </StackPanel>
            
            <Border Margin="0,20,0,0" Background="#F0F0F0" Padding="10" BorderThickness="1" BorderBrush="#E0E0E0">
                <StackPanel>
                    <TextBlock FontSize="14" FontWeight="SemiBold" Margin="0,0,0,10">当前配置</TextBlock>
                    <TextBlock x:Name="ConfigInfo" FontFamily="Consolas" FontSize="12"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>