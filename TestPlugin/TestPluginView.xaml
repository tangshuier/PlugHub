<UserControl x:Class="TestPlugin.TestPluginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TestPlugin"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Padding="10">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="测试插件" FontSize="20" FontWeight="Bold"/>
        </StackPanel>
        
        <TabControl Grid.Row="1">
            <!-- 日志测试 -->
            <TabItem Header="日志测试">
                <StackPanel Grid.Row="1" Margin="10">
                    <TextBlock Text="测试日志记录功能" FontWeight="Bold" Margin="0,0,0,10"/>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="记录调试信息" Width="120" Click="OnDebugLogClick" Margin="0,0,10,0"/>
                        <Button Content="记录信息" Width="120" Click="OnInfoLogClick" Margin="0,0,10,0"/>
                        <Button Content="记录警告" Width="120" Click="OnWarnLogClick" Margin="0,0,10,0"/>
                        <Button Content="记录错误" Width="120" Click="OnErrorLogClick"/>
                    </StackPanel>
                    <TextBlock Text="查看主应用的调试窗口以查看日志输出" FontStyle="Italic" Foreground="Gray" Margin="0,10,0,0"/>
                </StackPanel>
            </TabItem>
            
            <!-- 文件操作测试 -->
            <TabItem Header="文件操作测试">
                <StackPanel Margin="10">
                    <TextBlock Text="测试文件操作功能" FontWeight="Bold" Margin="0,0,0,10"/>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="创建测试文件" Width="120" Click="OnCreateFileClick" Margin="0,0,10,0"/>
                        <Button Content="写入文件" Width="120" Click="OnWriteFileClick" Margin="0,0,10,0"/>
                        <Button Content="读取文件" Width="120" Click="OnReadFileClick" Margin="0,0,10,0"/>
                        <Button Content="检查文件存在" Width="150" Click="OnCheckFileExistsClick"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <Button Content="删除测试文件" Width="120" Click="OnDeleteFileClick" Margin="0,0,10,0"/>
                        <Button Content="创建目录" Width="120" Click="OnCreateDirectoryClick" Margin="0,0,10,0"/>
                        <Button Content="检查目录存在" Width="150" Click="OnCheckDirectoryExistsClick" Margin="0,0,10,0"/>
                        <Button Content="搜索文件" Width="120" Click="OnSearchFilesClick"/>
                    </StackPanel>
                    <TextBlock x:Name="FileOperationResult" Text="" Margin="0,10,0,0"/>
                </StackPanel>
            </TabItem>
            
            <!-- 窗口操作测试 -->
            <TabItem Header="窗口操作测试">
                <StackPanel Margin="10">
                    <TextBlock Text="测试窗口操作功能" FontWeight="Bold" Margin="0,0,0,10"/>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="创建非模态窗口" Width="150" Click="OnCreateNonModalWindowClick" Margin="0,0,10,0"/>
                        <Button Content="创建模态窗口" Width="150" Click="OnCreateModalWindowClick"/>
                    </StackPanel>
                    <TextBlock x:Name="WindowOperationResult" Text="" Margin="0,10,0,0"/>
                </StackPanel>
            </TabItem>
            
            <!-- 插件信息测试 -->
            <TabItem Header="插件信息">
                <StackPanel Margin="10">
                    <TextBlock Text="插件信息测试" FontWeight="Bold" Margin="0,0,0,10"/>
                    <StackPanel>
                        <TextBlock Margin="0,0,0,5"><Run FontWeight="Bold">插件ID:</Run> <Run x:Name="PluginIdText"/></TextBlock>
                        <TextBlock Margin="0,0,0,5"><Run FontWeight="Bold">插件名称:</Run> <Run x:Name="PluginNameText"/></TextBlock>
                        <TextBlock TextWrapping="Wrap"><Run FontWeight="Bold">插件路径:</Run> <Run x:Name="PluginPathText"/></TextBlock>
                    </StackPanel>
                </StackPanel>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>